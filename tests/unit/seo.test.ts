import { describe, expect, it } from 'vitest';
import { absoluteUrl, pageTitle, seoFor } from '../../src/lib/seo';
describe('SEO', () => { it('creates production canonical URLs', () => expect(absoluteUrl('/produtos/banco')).toBe('https://santanafitness.com.br/produtos/banco/')); it('formats browser titles without separators', () => { expect(pageTitle()).toBe('Santana Fitness'); expect(pageTitle('Produtos')).toBe('Produtos Santana Fitness'); expect(pageTitle('Produtos')).not.toContain('|'); }); it('returns canonical metadata', () => expect(seoFor('Produtos', 'Catálogo', '/produtos').canonical).toContain('https://santanafitness.com.br/')); });
